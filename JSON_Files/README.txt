Use this folder for hosting different purpose JSON files, then simply copy the purpose into the main folder and rename it to "purpose.json".

capabilities.json can be swapped in and out as additional features are implemented.